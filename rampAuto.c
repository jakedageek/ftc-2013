#pragma config(Hubs,  S1, HTMotor,  HTServo,  HTMotor,  HTMotor)
#pragma config(Sensor, S1,     ,               sensorI2CMuxController)
#pragma config(Sensor, S2,     Sonar,          sensorSONAR)
#pragma config(Motor,  mtr_S1_C1_1,     driveLeft,     tmotorTetrix, openLoop, reversed)
#pragma config(Motor,  mtr_S1_C1_2,     liftLeft,      tmotorTetrix, openLoop, encoder)
#pragma config(Motor,  mtr_S1_C3_1,     driveRight,    tmotorTetrix, openLoop)
#pragma config(Motor,  mtr_S1_C3_2,     liftRight,     tmotorTetrix, openLoop, encoder)
#pragma config(Motor,  mtr_S1_C4_1,     intake,        tmotorTetrix, openLoop)
#pragma config(Motor,  mtr_S1_C4_2,     motorI,        tmotorTetrix, openLoop)
#pragma config(Servo,  srvo_S1_C2_1,    servoGate,               tServoNormal)
#pragma config(Servo,  srvo_S1_C2_2,    hookLeft,               tServoNormal)
#pragma config(Servo,  srvo_S1_C2_3,    hookRight,               tServoNormal)
#pragma config(Servo,  srvo_S1_C2_4,    servo4,               tServoNone)
#pragma config(Servo,  srvo_S1_C2_5,    servo5,               tServoNone)
#pragma config(Servo,  srvo_S1_C2_6,    servo6,               tServoNone)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//

#include "util.h"
#include "lift.h"
#include "autonomous3.h"

//RAMP AUTONOMOUS

void initializeRobot(){
	calibrateGyro();

	//RESET SERVOS

	return;
}

task main(){
	initializeRobot();
	waitForStart();

	//drive backwards off the ramp
	driveBackward(800, 50);
	//keep driving
	driveBackward(1000);
	//stop infront of the tube
	driveStop();
	//hook the tube
	hook(false);
	//raise the arm
	liftMove(SIXTY_LIFT, true);
	//open the gate
	gate(true);
	//turn
	turnEuler(45, 35, false);
	//drive forward to the goal
	driveForward(4000);

}
